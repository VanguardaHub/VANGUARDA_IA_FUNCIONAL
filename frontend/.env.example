# ============================================================
# Vanguarda.IA — configuração do frontend
# Copie para frontend/.env. Variáveis REACT_APP_* são embutidas
# no bundle em tempo de build — nunca coloque segredos aqui.
# ============================================================

# URL do backend, sem barra final (OBRIGATÓRIO)
REACT_APP_BACKEND_URL=http://localhost:8001
